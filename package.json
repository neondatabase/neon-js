{
  "name": "neon-js-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "link:neon-js": "cd packages/auth && bun link && cd ../postgrest-js && bun link && cd ../auth-ui && bun link && cd ../neon-js && bun link",
    "build": "bun run --filter '@neondatabase/postgrest-js' build && bun run --filter '@neondatabase/auth-ui' build && bun run --filter '@neondatabase/auth' build && bun run --filter '@neondatabase/neon-js' build",
    "dev": "bun run --filter '@neondatabase/postgrest-js' dev & bun run --filter '@neondatabase/auth' dev & bun run --filter '@neondatabase/auth-ui' dev & bun run --filter '@neondatabase/neon-js' dev",
    "test": "bun run --filter '@neondatabase/postgrest-js' test && bun run --filter '@neondatabase/auth' test && bun run --filter '@neondatabase/auth-ui' test && bun run --filter '@neondatabase/neon-js' test",
    "test:node": "bun run --filter '@neondatabase/postgrest-js' test:node && bun run --filter '@neondatabase/auth' test:node && bun run --filter '@neondatabase/auth-ui' test:node && bun run --filter '@neondatabase/neon-js' test:node",
    "test:ci": "bun run --filter '@neondatabase/postgrest-js' test:ci && bun run --filter '@neondatabase/auth' test:ci && bun run --filter '@neondatabase/auth-ui' test:ci && bun run --filter '@neondatabase/neon-js' test:ci",
    "typecheck": "bun run --filter '*' typecheck",
    "test:types": "bun run --filter '*' test:types",
    "lint": "bun run --filter '@neondatabase/*' lint",
    "clean": "bun run --filter '@neondatabase/*' clean && rm -rf node_modules",
    "release": "bun run build && bun run --filter '@neondatabase/postgrest-js' release && bun run --filter '@neondatabase/auth' release && bun run --filter '@neondatabase/auth-ui' release && bun run --filter '@neondatabase/neon-js' release",
    "release:postgrest-js": "bun scripts/release.ts postgrest-js",
    "release:auth": "bun scripts/release.ts auth",
    "release:auth-ui": "bun scripts/release.ts auth-ui",
    "release:neon-js": "bun scripts/release.ts neon-js"
  },
  "devDependencies": {
    "@arethetypeswrong/core": "^0.18.2",
    "@eslint/js": "^9.39.1",
    "@types/node": "24.7.2",
    "bumpp": "10.1.0",
    "eslint": "^9.39.1",
    "eslint-plugin-unicorn": "^62.0.0",
    "msw": "2.6.8",
    "publint": "^0.3.15",
    "tsdown": "0.17.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.49.0",
    "vite-tsconfig-paths": "5.1.4",
    "vitest": "4.0.7"
  },
  "dependencies": {}
}
