{
  "name": "neon-js-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "link:neon-js": "cd packages/neon-auth && bun link && cd ../postgrest-js && bun link && cd ../neon-js && bun link",
    "build": "bun run --filter '@neondatabase/postgrest-js' build && bun run --filter '@neondatabase/neon-auth' build && bun run --filter '@neondatabase/neon-js' build",
    "dev": "bun run --filter '@neondatabase/postgrest-js' dev & bun run --filter '@neondatabase/neon-auth' dev & bun run --filter '@neondatabase/neon-js' dev",
    "test": "bun run --filter '@neondatabase/postgrest-js' test && bun run --filter '@neondatabase/neon-auth' test && bun run --filter '@neondatabase/neon-js' test",
    "test:node": "bun run --filter '@neondatabase/postgrest-js' test:node && bun run --filter '@neondatabase/neon-auth' test:node && bun run --filter '@neondatabase/neon-js' test:node",
    "test:ci": "bun run --filter '@neondatabase/postgrest-js' test:ci && bun run --filter '@neondatabase/neon-auth' test:ci && bun run --filter '@neondatabase/neon-js' test:ci",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter '@neondatabase/neon-js' lint",
    "release": "bun run build && bun run --filter '@neondatabase/postgrest-js' release && bun run --filter '@neondatabase/neon-auth' release && bun run --filter '@neondatabase/neon-js' release"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "24.7.2",
    "bumpp": "10.1.0",
    "eslint": "^9.39.1",
    "eslint-plugin-unicorn": "^62.0.0",
    "msw": "2.6.8",
    "tsdown": "0.16.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.4",
    "vite-tsconfig-paths": "5.1.4",
    "vitest": "4.0.7"
  },
  "dependencies": {
    "@supabase/auth-js": "2.79.0",
    "@supabase/postgres-meta": "0.93.1",
    "@supabase/postgrest-js": "2.79.0",
    "better-auth": "1.3.34",
    "meow": "14.0.0",
    "zod": "4.1.12"
  }
}
