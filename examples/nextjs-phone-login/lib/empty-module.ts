// Stub for optional captcha packages that aren't installed.
// The auth UI bundle dynamically imports these; this empty module
// prevents Turbopack from failing at resolution time.
export default {};
