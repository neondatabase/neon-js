# Neon Auth Configuration
NEON_AUTH_BASE_URL=https://your-project.neon.tech
NEON_AUTH_COOKIE_SECRET=your-secret-at-least-32-characters-long

# Database URL (for main app with Drizzle ORM)
DATABASE_URL=postgresql://user:password@host/database

# Cookie domain for cross-subdomain authentication
# This MUST be set to enable cookie sharing between subdomains
# Use .myapp.local for local testing
COOKIE_DOMAIN=.myapp.local

# Main app URL for redirects (used by admin portal for non-admin users)
MAIN_APP_URL=https://app.myapp.local

# How to use:
# 1. Copy this file to .env: cp .env.docker.example .env
# 2. Fill in your credentials above
# 3. Run: docker-compose up --build
#
# Note: Docker Compose automatically reads .env from the current directory
